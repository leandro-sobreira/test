enum ProfileId {
  teacher = 1,
  student = 2,
  other = 3,
}

enum AnswerStatus {
  correct = 1,
  incorrect = 2,
  error = 3,
  running = 4,
  notAnswered = 5,
}

export { ProfileId, AnswerStatus }
